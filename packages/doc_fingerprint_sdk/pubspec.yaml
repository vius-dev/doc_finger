name: doc_fingerprint_sdk
description: Official Dart SDK for the Document Fingerprint API. Register, verify, and manage document fingerprints.
version: 1.0.0
homepage: https://docfingerprint.com
repository: https://github.com/docfingerprint/dart-sdk

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.0
  crypto: ^3.0.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
